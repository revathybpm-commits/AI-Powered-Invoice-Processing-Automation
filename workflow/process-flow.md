# Invoice Processing Workflow

1. User uploads invoice.
2. AI extracts invoice details.
3. Business rules check invoice amount.
4. If amount < $1000 → auto approval.
5. If amount > $1000 → manager approval required.
